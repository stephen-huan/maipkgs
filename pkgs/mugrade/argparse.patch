--- a/setup.py
+++ b/setup.py
@@ -14,7 +14,7 @@ setup(
     description="Interface library for minimalist autograding site",
     python_requires=">=3.5",
     url="http://github.com/locuslab/mugrade",
-    install_requires=["numpy >= 1.15", "pytest", "argparse >= 1.1", "requests"],
+    install_requires=["numpy >= 1.15", "pytest", "requests"],
     zip_safe=False,
 )
 
